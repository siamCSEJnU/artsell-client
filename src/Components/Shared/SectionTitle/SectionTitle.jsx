const SectionTitle = ({ heading }) => {
  return (
    <div className="text-center my-8">
      <h2 className="text-3xl text-slate-800 font-bold">{heading}</h2>
    </div>
  );
};

export default SectionTitle;
